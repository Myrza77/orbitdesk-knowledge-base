
  /* ===== additions for the knowledge base editor (Section 3 / 4.1 / bell / alert) ===== */
  body{ background:var(--bg-side); }
  .topbar{display:flex; align-items:center; gap:12px; padding:14px 22px; border-bottom:1px solid var(--border); flex-wrap:wrap; background:#fff;}
  .topbar h1{font-size:16px; margin:0; font-weight:700; white-space:nowrap; letter-spacing:-.01em;}
  .topbar .sub{color:var(--text2); font-size:12.5px; margin-right:auto;}
  .demo-banner{background:var(--warn-bg); color:#92600A; border:1px solid #FDE7B8; border-radius:8px; padding:4px 10px; font-size:12px; font-weight:600;}
  .warn-bg{background:#FFFBEB;} .warn{color:#D97706;}
  .app-wrap{max-width:1180px; margin:0 auto; padding:22px;}

  .bell-wrap{position:relative;}
  .bell-btn{width:34px; height:34px; border-radius:50%; border:1px solid var(--border); background:#fff; cursor:pointer; font-size:15px; position:relative; transition:border-color .15s ease, box-shadow .15s ease;}
  .bell-btn:hover{border-color:var(--accent-soft-border); box-shadow:0 0 0 3px var(--accent-soft);}
  .bell-badge{position:absolute; top:-4px; right:-4px; background:var(--bad); color:#fff; font-size:10px; font-weight:800; border-radius:8px; padding:1px 5px;}
  .bell-panel{position:absolute; right:0; top:42px; width:340px; background:#fff; border:1px solid var(--border); border-radius:12px; box-shadow:0 12px 32px rgba(17,24,39,.14); padding:14px; z-index:20;}
  .bell-panel h4{margin:0 0 4px; font-size:13px;}
  .bell-panel .sub{font-size:11.5px; color:var(--text2); margin-bottom:10px;}
  .digest-row{display:flex; justify-content:space-between; padding:7px 0; border-bottom:1px solid var(--border-soft); font-size:12.5px;}
  .digest-row:last-child{border-bottom:none;}
  .digest-count{font-weight:700; color:var(--accent);}
  .digest-action{margin-top:10px; font-size:11.5px; color:var(--text2); background:var(--bg-side); border-radius:8px; padding:8px 10px;}

  .alert-banner{background:var(--warn-bg); border:1px solid #FDE7B8; border-radius:10px; padding:10px 14px; margin-bottom:14px; font-size:12.5px; color:#92600A; display:flex; align-items:center; gap:10px; cursor:pointer; transition:box-shadow .15s ease;}
  .alert-banner:hover{box-shadow:0 2px 10px rgba(217,119,6,.15);}
  .alert-banner b{font-weight:700;}
  .alert-banner .go{margin-left:auto; color:var(--accent); font-weight:700; white-space:nowrap;}

  .kb-cols{display:grid; grid-template-columns:230px 260px 1fr; gap:16px; align-items:start;}
  .kb-cols.collapsed{grid-template-columns:1fr 1fr;}
  .kb-col{background:#fff; border:1px solid var(--border); border-radius:var(--radius); overflow:hidden; max-height:640px; overflow-y:auto; box-shadow:0 1px 2px rgba(16,24,40,.03);}
  .kb-col.hide{display:none;}
  .kb-col-head{padding:11px 14px; font-size:11px; color:var(--text3); text-transform:uppercase; letter-spacing:.05em; font-weight:700; border-bottom:1px solid var(--border-soft); background:var(--bg-side); position:sticky; top:0;}
  .kb-item{padding:10px 14px; font-size:13px; border-bottom:1px solid var(--border-soft); border-left:3px solid transparent; cursor:pointer; transition:background .12s ease, border-color .12s ease;}
  .kb-item:last-child{border-bottom:none;}
  .kb-item:hover{background:#F9FAFB;}
  .kb-item.active{background:var(--accent-soft); color:var(--accent); border-left-color:var(--accent); font-weight:600;}
  .kb-item .kb-item-meta{font-size:10.5px; color:var(--text3); margin-top:2px; font-weight:400;}
  .kb-item.active .kb-item-meta{color:var(--accent);}

  .edit-panel{background:#fff; border:1px solid var(--border); border-radius:var(--radius); padding:20px; box-shadow:0 1px 2px rgba(16,24,40,.03);}
  .edit-title-row{display:flex; align-items:center; justify-content:space-between; gap:10px; margin-bottom:4px;}
  .edit-title{font-size:15.5px; font-weight:700; letter-spacing:-.01em;}
  .edit-traffic{font-size:11.5px; color:var(--text2); margin-bottom:14px;}
  .chip-row{display:flex; gap:8px; margin-bottom:12px; flex-wrap:wrap; align-items:center;}
  .chip{font-size:11.5px; padding:5px 11px; border-radius:16px; border:1px solid var(--border); color:var(--text2); background:#fff; display:flex; align-items:center; gap:5px; cursor:pointer; font-weight:600; transition:border-color .12s ease, background .12s ease;}
  .chip:hover{border-color:var(--accent-soft-border);}
  .chip.ok{border-color:var(--ok); color:var(--ok); background:var(--ok-bg);}
  .chip.warn{border-color:#FDE7B8; color:var(--warn); background:var(--warn-bg);}
  .chip.unk{border-color:var(--border); color:var(--text2); background:#fff;}
  .chip.active{outline:2px solid var(--accent); outline-offset:1px;}
  .chip-detail-box{background:var(--bg-side); border:1px solid var(--border); border-radius:8px; padding:10px 12px; font-size:12px; color:var(--text2); margin-bottom:12px; line-height:1.5;}
  .source-badge{font-size:10.5px; font-weight:700; padding:2px 8px; border-radius:6px;}
  .source-badge.external{background:var(--ok-bg); color:var(--ok); border:1px solid #BBF0CE;}
  .source-badge.internal{background:var(--muted-bg); color:var(--text2); border:1px solid var(--border);}
  .legal-note{font-size:12px; color:var(--text2); background:var(--bg-side); border-radius:8px; padding:9px 11px; margin-bottom:12px; line-height:1.5;}

  /* inline pointers inside the edit panel -- divergence (🔀) and manager-pattern (👥) alerts */
  .alert{border-radius:10px; padding:9px 12px; font-size:12px; margin-bottom:12px; border:1px solid;}
  .alert.div{background:var(--div-bg); border-color:var(--div-border); color:#4C1D95;}
  .alert.dup{background:var(--bad-bg); border-color:#FBD5D5; color:#9B1C1C;}
  .alert.compact{cursor:pointer; font-weight:700; transition:filter .12s ease;}
  .alert.compact:hover{filter:brightness(0.97);}

  /* Law statement list (4.1 panel + chip detail) */
  .legal-mini{padding:8px 0; border-top:1px dashed var(--border); display:flex; align-items:flex-start; gap:8px; flex-wrap:wrap;}
  .legal-mini:first-child{border-top:none;}
  .legal-mini .txt{flex:1; min-width:160px; font-size:12px; color:var(--text);}
  .verdict{font-size:10px; font-weight:700; padding:2px 6px; border-radius:5px; margin-right:5px; white-space:nowrap; flex:none;}
  .verdict.neverno{background:var(--bad-bg); color:var(--bad);}
  .verdict.somnitelno{background:var(--warn-bg); color:var(--warn);}
  .verdict.verno{background:var(--ok-bg); color:var(--ok);}

  /* history + shared-block tabs under the editor */
  .sec-tabs{display:flex; gap:0; margin-bottom:0; border-bottom:1px solid var(--border);}
  .sec-tab{border:none; background:transparent; padding:9px 4px; margin-right:16px; font-size:12px; font-weight:600; color:var(--text2); cursor:pointer; border-bottom:2px solid transparent; transition:color .12s ease, border-color .12s ease;}
  .sec-tab:hover{color:var(--text);}
  .sec-tab.active{color:var(--accent); border-bottom-color:var(--accent);}
  .sec-panel{padding-top:12px;}
  .history-row{padding:10px 0; border-top:1px solid var(--border-soft); display:flex; align-items:flex-start; justify-content:space-between; gap:10px; font-size:12px; color:var(--text2);}
  .history-row:first-child{border-top:none;}
  .history-row .hr-text{flex:1;}
  .history-row .restore-link{color:var(--accent); cursor:pointer; font-size:11px; white-space:nowrap; flex:none; font-weight:600;}
  .history-row .restore-link:hover{text-decoration:underline;}
  .hist-diff{margin-top:5px;}
  .hist-diff summary{font-size:11px; color:var(--accent); cursor:pointer; list-style:none;}
  .hist-diff summary::-webkit-details-marker{display:none;}
  .diff-box{font-size:11.5px; background:var(--bg-side); border:1px solid var(--border); border-radius:6px; padding:8px 10px; margin-top:6px; line-height:1.6;}
  del{color:#B91C1C; background:#FEE2E2; text-decoration:line-through; padding:0 1px; border-radius:3px;}
  ins{color:#166534; background:#DCFCE7; text-decoration:none; padding:0 1px; border-radius:3px;}
  .shared-block-panel .used-in{font-size:11px; color:var(--text2); margin-top:8px;}
  .shared-block-panel .used-in a{color:var(--accent); cursor:pointer; text-decoration:none; font-weight:600;}
  .shared-block-panel .used-in a:hover{text-decoration:underline;}

  /* status pill: draft vs published, same visual language as the manager scorecard's status-pill-online */
  .status-pill{display:inline-flex; align-items:center; gap:5px; font-size:11.5px; font-weight:700; padding:4px 10px; border-radius:999px; flex:none; white-space:nowrap;}
  .status-pill.pub{background:var(--ok-bg); color:var(--ok);}
  .status-pill.draft{background:var(--warn-bg); color:#92600A;}

  /* editable instruction text -- replaces the old read-only .edit-body div */
  .editor-shell{border:1px solid var(--border); border-radius:10px; overflow:hidden; margin-bottom:8px; transition:border-color .15s ease, box-shadow .15s ease;}
  .editor-shell.dirty{border-color:var(--accent-soft-border); box-shadow:0 0 0 3px var(--accent-soft);}
  .editor-toolbar{display:flex; align-items:center; justify-content:space-between; gap:10px; padding:8px 12px; background:var(--bg-side); border-bottom:1px solid var(--border); font-size:11px; font-weight:700; color:var(--text2); text-transform:uppercase; letter-spacing:.03em;}
  .editor-toolbar .editor-hint{font-size:10.5px; font-weight:400; text-transform:none; letter-spacing:0; color:var(--text3); text-align:right;}
  .editor-textarea{display:block; width:100%; min-height:170px; border:none; outline:none; resize:vertical; font-family:inherit; font-size:12.5px; line-height:1.7; color:var(--text); padding:12px; background:#fff;}
  .editor-textarea:focus{background:#fff;}
  .save-row{display:flex; align-items:center; gap:8px; margin-bottom:14px; min-height:28px; flex-wrap:wrap;}
  .save-row .save-status{font-size:11.5px; color:var(--text3);}
  .save-row .save-status.ok{color:var(--ok); font-weight:600;}
  .draft-banner{display:flex; align-items:center; justify-content:space-between; gap:10px; background:var(--warn-bg); border:1px solid #FDE7B8; color:#92600A; border-radius:8px; padding:9px 12px; margin-bottom:14px; font-size:12px; font-weight:600;}
  .draft-banner .btns{display:flex; gap:6px; flex:none;}

  .entry-btns{display:flex; gap:8px; flex-wrap:wrap;}
  .entry-btn{flex:1; min-width:150px; font-family:inherit; font-size:12px; font-weight:600; padding:10px 12px; border-radius:9px; border:1px solid var(--border); background:#fff; color:var(--text); cursor:pointer; text-align:left; transition:border-color .15s ease, box-shadow .15s ease, transform .1s ease;}
  .entry-btn:hover{border-color:var(--accent); box-shadow:0 2px 8px rgba(91,79,229,.1);}
  .entry-btn:active{transform:translateY(1px);}
  .entry-btn .sub2{display:block; font-size:10px; color:var(--text3); font-weight:400; margin-top:2px;}
  .kb-slot{margin-top:14px;}

  .ai-panel .match-row{display:flex; align-items:center; gap:12px; margin-bottom:16px; padding:12px 14px; border-radius:10px; background:var(--bg-side);}
  .ai-panel .match-num{font-size:24px; font-weight:800;}
  .compare-grid{display:grid; grid-template-columns:1fr; gap:10px;}
  .compare-item{border:1px solid var(--border); border-radius:9px; padding:10px 12px;}
  .compare-item .label{font-size:10px; text-transform:uppercase; color:var(--text3); font-weight:700; margin-bottom:4px;}
  .compare-item .val{font-size:12.5px; line-height:1.5;}
  .compare-item.q{background:var(--accent-soft);}
  .compare-item.etalon{background:var(--ok-bg); border-color:#BBF0CE;}

  /* general polish: consistent focus rings + hover transitions across reused controls */
  .btn, .btn.secondary, .btn.ghost{transition:filter .12s ease, box-shadow .12s ease, transform .1s ease;}
  .btn:hover{filter:brightness(1.06);}
  .btn.secondary:hover, .btn.ghost:hover{background:var(--accent-soft);}
  .btn:active{transform:translateY(1px);}
  .qs-pill{transition:background .12s ease, color .12s ease, border-color .12s ease;}
  .qs-pill:not(.current):not(.disabled):hover{border-color:var(--accent-soft-border); background:var(--accent-soft);}
